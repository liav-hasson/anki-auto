# TODO

## [Planned]

- **IMPORTANT**
  - Prompt.py overhall and reconsideration
  - Add example files for blacklist + customizations (consider examples dir)
  - pre-run prompt is not showing if sound is turned on or off.
  - remove "example" notes sections, prefer sentences examples directly under the noted words themselfs
  - this shouldnt exist: "When `ANKI_MINIMAL_CARDS=true`, customization can still affect the core concept and main
examples, but no built-in or custom notes are generated or rendered."
  - Force wrapping sections with template keys (e.g. `(())`, `[[]]`, `{{}}`) for consistant later visual formatting (italic, bolds, colors)
  - Align [INFO] logging line spacing pre run.
  - Increase AI reasoning + allow web search tool
  - Run a grilling session after finishing.
- **AI features**
  - Randomize AI voice setting.
  - Allow other AI providers using litellm
  - Return token / cost metadata
  - Add image generation toggle for notes
- **Prompt features**
  - Seperate default notes per level (A1-2 should show conjugation, explain readings - while B1+ explain deeper concepts)
- **Visual**
  - Progress bar + tracking time.
